# Installation

```bash
unzip nebenkostenabrechnung-1.0.0.zip
cd nebenkostenabrechnung
sudo ./install.sh
```

Weboberfläche: `http://SERVER-IP:8080`
Datenbank: `/var/lib/nebenkostenabrechnung/nebenkosten.db`
